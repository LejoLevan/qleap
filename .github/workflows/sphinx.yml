name: "Sphinx: Render docs"

on: 
  push:
    branch:
      - feature-stable

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - uses: actions/checkout@v4
      with:
        persist-credentials: false
