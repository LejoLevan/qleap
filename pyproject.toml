[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"

[project]
name = "qpp_test"
version = "0.0.2"
dependencies = [
    "qiskit>=1.0,<2.0",
    "qiskit-aer>=0.13,<1.0"
]
